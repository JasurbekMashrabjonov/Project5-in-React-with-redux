
const initialState = {
    class1:''
}

function reducer(state = initialState,{type,payload}) {
   switch (type) {
    case "":
        
        break;
   
    default:
        return state
   }
}

export default reducer;